import {useEffect,useState} from 'react';import {Bell,Check,TriangleAlert} from 'lucide-react';import {Button,Card,Modal} from './UI';import {Link} from 'react-router-dom';import {useApp} from '../../context/AppContext';
export function NotificationBell(){const{notifications}=useApp();return <Link to="/notifications" className="relative"><Bell size={19}/></Link>}
export function Loading({text='Loading...'}){return <div className="flex min-h-40 items-center justify-center gap-3 text-sm text-slate-500"><span className="h-5 w-5 animate-spin rounded-full border-2 border-[#B7C9B1] border-t-[#243B35]"/>{text}</div>}
export function useToast(){const[t,setT]=useState('');useEffect(()=>{if(t){const x=setTimeout(()=>setT(''),2800);return()=>clearTimeout(x)}},[t]);return[t,setT]}
export function Confirm({open,onClose,onConfirm,title='Delete record',text='This action cannot be undone.'}){return <Modal open={open} onClose={onClose} title={title}><div className="text-sm text-slate-600">{text}</div><div className="mt-6 flex justify-end gap-2"><Button variant="ghost" onClick={onClose}>Cancel</Button><Button variant="danger" onClick={()=>{onConfirm();onClose()}}>Delete</Button></div></Modal>}
export function Disclaimer(){return <div className="flex gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900"><TriangleAlert className="mt-0.5 shrink-0" size={18}/><div><b>Important:</b> MastiSense AI is a prototype early-warning system. Its predictions are intended for demonstration and decision-support purposes only and are not a substitute for professional veterinary diagnosis.</div></div>}
